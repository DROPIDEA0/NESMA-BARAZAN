# 🚨 إرشادات صارمة للمطورين والوكلاء

**آخر تحديث:** 02 يناير 2026  
**الحالة:** إلزامي للجميع

## ⚠️ قواعد إلزامية - يُمنع مخالفتها

### 1. ملفات التوثيق (Documentation)

> **القاعدة الذهبية:** ملف توثيق واحد فقط في جذر المشروع

- ✅ **الملف الوحيد المسموح:** `UNIFIED_DOCUMENTATION.md`
- ❌ **ممنوع منعاً باتاً:**
  - إنشاء أي ملفات `.md` أخرى في جذر المشروع
  - إنشاء ملفات مثل: `README.md`, `TODO.md`, `DEPLOYMENT.md`, `NOTES.md`, إلخ
  - نسخ أو تكرار محتوى التوثيق في ملفات منفصلة

#### كيفية التحديث
```bash
# عند إضافة معلومات جديدة
1. افتح ملف UNIFIED_DOCUMENTATION.md
2. ابحث عن القسم المناسب
3. أضف أو عدّل المعلومات في نفس الملف
4. لا تنشئ ملفات جديدة
```

#### عند كل Push إلى GitHub
- حدّث `UNIFIED_DOCUMENTATION.md` بالمعلومات الجديدة فقط
- لا تضف ملفات `.md` جديدة
- تأكد من حذف أي ملفات `.md` إضافية قبل الـ Push

---

### 2. قاعدة البيانات (Database)

> **القاعدة الذهبية:** ملف SQL واحد فقط في جذر المشروع

- ✅ **الملف الوحيد المسموح:** `DATABASE.sql`
- ❌ **ممنوع منعاً باتاً:**
  - رفع ملفات SQL إضافية مثل: `schema.sql`, `migration.sql`, `backup.sql`, إلخ
  - رفع نسخ احتياطية لقاعدة البيانات إلى GitHub
  - إنشاء ملفات SQL منفصلة للتحديثات

#### كيفية التحديث
```bash
# عند تحديث بنية قاعدة البيانات
1. قم بتصدير قاعدة البيانات الكاملة من السيرفر
2. استبدل محتوى ملف DATABASE.sql بالتصدير الجديد
3. لا تنشئ ملفات SQL جديدة
```

#### أمر التصدير من السيرفر
```bash
ssh root@72.62.7.159
mysqldump -u u521934522-nasma-db-new -pDowny144168@144168 -h 127.0.0.1 u521934522-nasma-db > DATABASE.sql
```

---

### 3. بيئة الإنتاج (Production Environment)

> **القاعدة الذهبية:** استخدم المعلومات المحدثة فقط من UNIFIED_DOCUMENTATION.md

#### معلومات السيرفر (VPS)
```
IP: 72.62.7.159
المسار: /home/shheercom/htdocs/www.shheer.com
SSH: ssh root@72.62.7.159
كلمة المرور: Downy144168@#144168
```

#### معلومات قاعدة البيانات
```
Host: 127.0.0.1
Port: 3306
Database: u521934522-nasma-db
Username: u521934522-nasma-db-new
Password: Downy144168@144168
```

#### معلومات لوحة التحكم
```
الرابط: https://www.shheer.com/admin
اليوزر: admin
الباسورد: Bandar1#%
```

---

### 4. عملية النشر (Deployment)

> **القاعدة الذهبية:** استخدم سكريبت deploy.sh فقط

#### خطوات النشر الصحيحة
```bash
# 1. على جهازك المحلي
git add .
git commit -m "وصف التحديث"
git push origin main

# 2. على السيرفر
ssh root@72.62.7.159
cd /home/shheercom/htdocs/www.shheer.com
bash deploy.sh
```

#### ❌ لا تفعل
- لا تنشر مباشرة بدون استخدام سكريبت deploy.sh
- لا تعدّل الملفات مباشرة على السيرفر
- لا تنسى إعادة تشغيل الخدمة بعد التحديث

---

### 5. إدارة الخدمة (Service Management)

> **القاعدة الذهبية:** استخدم systemctl لإدارة الخدمة

#### الأوامر المهمة
```bash
# عرض حالة الخدمة
systemctl status nesma-barzan.service

# إعادة تشغيل الخدمة (بعد كل تحديث)
systemctl restart nesma-barzan.service

# عرض السجلات
journalctl -u nesma-barzan.service -f --no-pager
```

---

## 🔄 سير العمل الموصى به (Workflow)

### عند إضافة ميزة جديدة
1. ✅ اعمل على فرع منفصل (branch)
2. ✅ اختبر محلياً قبل الـ Push
3. ✅ حدّث `UNIFIED_DOCUMENTATION.md` بالمعلومات الجديدة
4. ✅ احذف أي ملفات `.md` أو `.sql` إضافية
5. ✅ اعمل Push للفرع
6. ✅ اعمل Merge للفرع الرئيسي (main)
7. ✅ نفذ سكريبت deploy.sh على السيرفر

### عند إصلاح مشكلة
1. ✅ شخّص المشكلة من السجلات (logs)
2. ✅ أصلح المشكلة في الكود
3. ✅ اختبر محلياً
4. ✅ حدّث التوثيق إذا لزم الأمر
5. ✅ اعمل Push
6. ✅ نفذ سكريبت deploy.sh على السيرفر

### عند تحديث قاعدة البيانات
1. ✅ عدّل البنية محلياً أو على السيرفر
2. ✅ صدّر قاعدة البيانات الكاملة
3. ✅ استبدل محتوى `DATABASE.sql`
4. ✅ حدّث التوثيق بالتغييرات
5. ✅ اعمل Push
6. ✅ نفذ سكريبت deploy.sh على السيرفر

---

## ❌ أخطاء شائعة يجب تجنبها

### 1. إنشاء ملفات توثيق إضافية
```bash
# ❌ خطأ
touch README.md
touch DEPLOYMENT.md
touch TODO.md

# ✅ صحيح
# عدّل UNIFIED_DOCUMENTATION.md فقط
```

### 2. رفع ملفات SQL إضافية
```bash
# ❌ خطأ
git add migration_v2.sql
git add backup_20260102.sql

# ✅ صحيح
# حدّث DATABASE.sql فقط
```

### 3. النشر بدون سكريبت deploy.sh
```bash
# ❌ خطأ
ssh root@72.62.7.159
cd /home/shheercom/htdocs/www.shheer.com
git pull
pnpm install
pnpm run build
# نسيت إعادة تشغيل الخدمة!

# ✅ صحيح
ssh root@72.62.7.159
cd /home/shheercom/htdocs/www.shheer.com
bash deploy.sh
```

### 4. استخدام معلومات قديمة
```bash
# ❌ خطأ - استخدام معلومات Shared Hosting القديمة
# ❌ خطأ - استخدام كلمة مرور قديمة

# ✅ صحيح
# استخدم المعلومات من UNIFIED_DOCUMENTATION.md فقط
```

---

## 📞 عند الحاجة للمساعدة

### 1. راجع التوثيق أولاً
- افتح `UNIFIED_DOCUMENTATION.md`
- ابحث عن القسم المناسب
- اتبع التعليمات بدقة

### 2. افحص السجلات (Logs)
```bash
# سجلات الخدمة
journalctl -u nesma-barzan.service -n 50 --no-pager

# سجلات Nginx
tail -f /var/log/nginx/error.log
```

### 3. تحقق من حالة الخدمات
```bash
# حالة الخدمة
systemctl status nesma-barzan.service

# حالة قاعدة البيانات
systemctl status mysql

# حالة Nginx
systemctl status nginx
```

---

## ✅ قائمة التحقق قبل كل Push

- [ ] هل حدّثت `UNIFIED_DOCUMENTATION.md` بالمعلومات الجديدة؟
- [ ] هل حذفت أي ملفات `.md` إضافية؟
- [ ] هل حذفت أي ملفات `.sql` إضافية (ما عدا DATABASE.sql)؟
- [ ] هل اختبرت التحديثات محلياً؟
- [ ] هل تأكدت من عدم وجود أخطاء في الكود؟
- [ ] هل كتبت رسالة commit واضحة؟

---

## 🎯 الهدف من هذه الإرشادات

**الحفاظ على:**
- ✅ توحيد التوثيق في مكان واحد
- ✅ سهولة الصيانة والتحديث
- ✅ تجنب التكرار والفوضى
- ✅ ضمان استخدام المعلومات الأحدث دائماً
- ✅ تسهيل عمل الفريق والوكلاء

**تجنب:**
- ❌ تشتت المعلومات في ملفات متعددة
- ❌ استخدام معلومات قديمة أو غير صحيحة
- ❌ صعوبة تتبع التحديثات
- ❌ الأخطاء الناتجة عن عدم التوحيد

---

**تذكر:** هذه الإرشادات إلزامية وليست اختيارية. الالتزام بها يضمن سير العمل بسلاسة وكفاءة.

---

**آخر تحديث:** 02 يناير 2026  
**الإصدار:** 1.0.0
