# Changelog

جميع التغييرات المهمة في مشروع نسمة برزان سيتم توثيقها في هذا الملف.

---

## [1.0.0] - 2025-12-29

### ✨ إضافات جديدة (Added)

#### نظام تسجيل الدخول
- إضافة صفحة تسجيل دخول بسيطة وآمنة (`/login`)
- إضافة صفحة تغيير كلمة المرور في لوحة التحكم (`/admin/change-password`)
- إضافة مستخدم افتراضي (admin/admin123) يتم إنشاؤه تلقائياً
- إضافة تشفير كلمات المرور باستخدام bcrypt
- إضافة حماية كاملة لجميع صفحات لوحة التحكم

#### Backend
- `server/auth.ts` - نظام المصادقة الكامل
  - `hashPassword()` - تشفير كلمات المرور
  - `verifyPassword()` - التحقق من كلمات المرور
  - `authenticateUser()` - مصادقة المستخدم
  - `createDefaultAdmin()` - إنشاء مستخدم افتراضي

- API Endpoints جديدة:
  - `auth.login` - تسجيل الدخول
  - `auth.changePassword` - تغيير كلمة المرور

- Database Functions:
  - `getUserById()` - الحصول على مستخدم بواسطة ID
  - `updateUser()` - تحديث بيانات المستخدم

#### Frontend
- `client/src/pages/Login.tsx` - صفحة تسجيل دخول احترافية
- `client/src/pages/admin/ChangePassword.tsx` - صفحة تغيير كلمة المرور
- `client/src/components/ProtectedRoute.tsx` - حماية الصفحات
- رابط "تغيير كلمة المرور" في القائمة الجانبية للوحة التحكم

#### قاعدة البيانات
- إضافة حقل `username` في جدول `users`
- إضافة حقل `password` في جدول `users`
- جعل حقل `openId` اختياري (nullable)
- ملف migration: `migrations/add_username_password.sql`

#### التوثيق
- `DEPLOYMENT_GUIDE.md` - دليل شامل للنشر على Hostinger
- `CHANGELOG.md` - توثيق جميع التغييرات
- `QUICK_SETUP_GUIDE.md` - دليل سريع للإعداد

### 🔧 تحسينات (Changed)

#### Schema
- تحديث `drizzle/schema.ts`:
  - `openId` أصبح اختياري (nullable)
  - إضافة `username` (unique)
  - إضافة `password` (nullable للمستخدمين OAuth)

#### AdminLayout
- تفعيل نظام الحماية (كان معطلاً في التطوير)
- إضافة رابط "تغيير كلمة المرور" في القائمة
- تحسين رسائل الخطأ عند عدم وجود صلاحيات

#### Server Initialization
- إضافة استدعاء `createDefaultAdmin()` عند بدء السيرفر
- التأكد من إنشاء مستخدم افتراضي تلقائياً

### 📦 Dependencies

#### إضافات
- `bcryptjs@^3.0.3` - تشفير كلمات المرور
- `@types/bcryptjs@^3.0.0` (dev) - أنواع TypeScript

### 🐛 إصلاحات (Fixed)

- إصلاح خطأ "Invalid URL" في الإنتاج (إضافة متغيرات `VITE_*`)
- إصلاح مشكلة import في `Login.tsx` (استخدام wouter بدلاً من react-router-dom)
- إصلاح استخدام toast (استخدام sonner بدلاً من use-toast)
- إصلاح import في `auth.ts` (استخدام getDb بدلاً من db مباشرة)

### 🔒 الأمان (Security)

- تشفير كلمات المرور باستخدام bcrypt (10 salt rounds)
- حماية جميع صفحات لوحة التحكم
- التحقق من صلاحيات المستخدم قبل الوصول
- Session-based authentication
- تحذير المستخدم لتغيير كلمة المرور الافتراضية

### 📝 ملاحظات

#### Environment Variables المطلوبة
```env
DATABASE_URL=mysql://...
SESSION_SECRET=...
NODE_ENV=production
VITE_OAUTH_PORTAL_URL=https://oauth.manus.im
VITE_APP_ID=nesma-barzan-prod
```

#### بيانات الدخول الافتراضية
```
Username: admin
Password: admin123
```

⚠️ **يجب تغيير كلمة المرور فوراً بعد أول تسجيل دخول!**

---

## [0.1.0] - 2025-12-28

### ✨ الإصدار الأولي

- إنشاء المشروع الأساسي
- تصميم الصفحة الرئيسية
- إنشاء لوحة التحكم
- إضافة إدارة المحتوى
- إضافة إدارة المشاريع
- إضافة إدارة الصور
- دعم اللغتين العربية والإنجليزية
- تصميم responsive
- دعم SQLite للتطوير
- دعم MySQL للإنتاج

---

## الإصدارات القادمة

### [1.1.0] - مخطط

- [ ] ربط Domain دائم
- [ ] إضافة SSL Certificate
- [ ] تحسين الأداء
- [ ] إضافة Google Analytics
- [ ] إضافة نظام النسخ الاحتياطي التلقائي

### [1.2.0] - مخطط

- [ ] إضافة نظام الإشعارات
- [ ] إضافة نظام الأدوار والصلاحيات المتقدم
- [ ] إضافة سجل النشاطات (Activity Log)
- [ ] إضافة نظام البحث المتقدم

---

## الروابط

- **الموقع:** https://mediumturquoise-dotterel-343079.hostingersite.com
- **GitHub:** https://github.com/DROPIDEA0/NESMA-BARAZAN
- **Hostinger:** https://hpanel.hostinger.com
